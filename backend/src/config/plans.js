const PLAN_LIMITS = {
  starter: 5000,
  pro: 20000,
  agency: 100000,
};

// Prices in CAD / month (for display / reporting)
const PLAN_PRICES = {
  starter: 50,
  pro: 150,
  agency: 400,
};

module.exports = { PLAN_LIMITS, PLAN_PRICES };

